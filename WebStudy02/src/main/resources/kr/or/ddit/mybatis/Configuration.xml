<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="kr/or/ddit/db/DbInfo.properties" />
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true" />
    </settings>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <!-- 커넥션을 생성하기 위해서 작성한 코드-->
            <dataSource type="POOLED">
                <property name="driver" value="${driverClassName}" />
                <property name="url" value="${url}" />
                <property name="username" value="${username}" />
                <property name="password" value="${password}" />
                <property name="poolMaximumActiveConnections" value="${maxPoolSize}" />
                <property name="poolMaximumIdleConnections" value="${minIdle}" />
                <property name="poolTimeToWait" value="${connectionTimeoutMs}" />
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="kr/or/ddit/mybatis/mapper/MemberMapper.xml" />
    </mappers>
</configuration>